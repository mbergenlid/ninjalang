package ninjalang;

class Array ofSize(size: Int) {
    constructor empty();
    val size: Int;

    native impure def get(x: Int): Any
    native impure def set(x: Int, v: Any): Unit

    native def copyWithNewSize(newSize: Int): Array
}