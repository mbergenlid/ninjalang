class Functions {
    def get(): Int = 5;
    def echo(x: Int): Int = x;

    val prop: Int = 42;

    def accessProperty(): Int = prop;

    def addOne(x: Int): Int = x + 1;

    def maxTen(x: Int): Int =
        if(x > 10) 10 else x

    impure def impureFunction(): Unit = {}
}